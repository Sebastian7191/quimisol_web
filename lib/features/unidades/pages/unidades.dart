import 'package:flutter/material.dart';

class UnidadesPage extends StatelessWidget {
  const UnidadesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Unidades', style: TextStyle(fontSize: 22, fontWeight: FontWeight.w800)),
    );
  }
}
